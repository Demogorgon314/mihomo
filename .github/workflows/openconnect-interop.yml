name: OpenConnect AnyConnect Interop

on:
  workflow_dispatch:
  schedule:
    - cron: "0 18 * * 0"
  push:
    tags:
      - "v*"
  pull_request:
    paths:
      - ".github/workflows/openconnect-interop.yml"
      - "adapter/outbound/openconnect*.go"
      - "adapter/parser.go"
      - "constant/adapters.go"
      - "internal/testutil/openconnect/**"
      - "transport/openconnect/**"
      - "go.mod"
      - "go.sum"

permissions:
  contents: read

jobs:
  hermetic-and-ocserv:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      CGO_ENABLED: 0
      GOTOOLCHAIN: local
      MIHOMO_ANYCONNECT_OCSERV: 1
    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c
        with:
          go-download-base-url: "https://github.com/MetaCubeX/go/releases/download/build"
          go-version: "1.25"

      - name: Check out pinned sing-openconnect fork
        uses: actions/checkout@v6
        with:
          repository: Demogorgon314/sing-openconnect
          ref: 04fd3c6219f94a42e083ef655ceb49d04e207d5b
          path: .anyconnect-upstream

      - name: Verify hermetic harness
        env:
          MIHOMO_ANYCONNECT_MATRIX: ${{ github.workspace }}/artifacts/hermetic-capabilities.json
        run: go test ./internal/testutil/openconnect -count=20

      - name: Verify hermetic harness with gVisor
        env:
          MIHOMO_ANYCONNECT_MATRIX: ${{ github.workspace }}/artifacts/gvisor-capabilities.json
        run: go test -tags=with_gvisor ./transport/openconnect ./adapter/outbound ./internal/testutil/openconnect -count=1

      - name: Verify AnyConnect lifecycle under race detector
        env:
          CGO_ENABLED: 1
        run: go test -race -tags=with_gvisor ./transport/openconnect ./adapter/outbound ./internal/testutil/openconnect -count=1

      - name: Enforce AnyConnect coverage floor
        run: |
          go test -tags=with_gvisor -coverprofile=/tmp/anyconnect-transport.cover ./transport/openconnect
          go test -tags=with_gvisor -coverprofile=/tmp/anyconnect-outbound.cover ./adapter/outbound
          go test -tags=with_gvisor -coverpkg=github.com/metacubex/mihomo/adapter/outbound -coverprofile=/tmp/anyconnect-parser.cover ./adapter
          go tool cover -func=/tmp/anyconnect-transport.cover | awk '/^total:/ { value=$3; sub(/%/, "", value); printf "transport/openconnect coverage: %.1f%%\n", value; if (value < 80) exit 1 }'
          awk 'FNR > 1 { statements[$1]=$2; if ($3 > counts[$1]) counts[$1]=$3 } END { for (block in statements) if (block ~ /adapter\/outbound\/openconnect(_session)?\.go:/) { total+=statements[block]; if (counts[block] > 0) covered+=statements[block] } value=100*covered/total; printf "OpenConnect adapter coverage: %.1f%%\n", value; if (value < 80) exit 1 }' /tmp/anyconnect-outbound.cover /tmp/anyconnect-parser.cover

      - name: Scan reachable Go vulnerabilities
        run: go run golang.org/x/vuln/cmd/govulncheck@v1.6.0 ./...

      - name: Verify pinned ocserv fixture
        env:
          MIHOMO_ANYCONNECT_MATRIX: ${{ github.workspace }}/artifacts/ocserv-capabilities.json
        run: go test -tags=anyconnect_ocserv,with_gvisor ./internal/testutil/openconnect -run '^TestOCServFixture$' -count=1 -v

      - name: Verify full upstream ocserv matrix
        working-directory: .anyconnect-upstream/test
        run: go test ./... -count=1 -timeout=15m

      - name: Upload capability matrices
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: anyconnect-capability-matrices
          path: artifacts/*.json
          if-no-files-found: error

  fuzz:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CGO_ENABLED: 0
      GOTOOLCHAIN: local
      FUZZ_TIME: ${{ github.event_name == 'schedule' && '2m' || startsWith(github.ref, 'refs/tags/') && '1m' || '10s' }}
    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c
        with:
          go-download-base-url: "https://github.com/MetaCubeX/go/releases/download/build"
          go-version: "1.25"

      - name: Check out pinned sing-openconnect fork
        uses: actions/checkout@v6
        with:
          repository: Demogorgon314/sing-openconnect
          ref: 04fd3c6219f94a42e083ef655ceb49d04e207d5b
          path: .anyconnect-upstream

      - name: Restore accumulated fuzz corpus
        uses: actions/cache@v5
        with:
          path: ~/.cache/go-build/fuzz
          key: anyconnect-fuzz-${{ runner.os }}-${{ hashFiles('go.sum', 'adapter/outbound/openconnect_generation_test.go', '.anyconnect-upstream/go.sum', '.anyconnect-upstream/anyconnect_parser_fuzz_test.go') }}-${{ github.run_id }}
          restore-keys: |
            anyconnect-fuzz-${{ runner.os }}-${{ hashFiles('go.sum', 'adapter/outbound/openconnect_generation_test.go', '.anyconnect-upstream/go.sum', '.anyconnect-upstream/anyconnect_parser_fuzz_test.go') }}-
            anyconnect-fuzz-${{ runner.os }}-

      - name: Fuzz protocol parsers
        working-directory: .anyconnect-upstream
        run: |
          go test -run '^$' -fuzz '^FuzzCSTPPacketParser$' -fuzztime="$FUZZ_TIME"
          go test -run '^$' -fuzz '^FuzzAnyConnectAuthenticationXML$' -fuzztime="$FUZZ_TIME"
          go test -run '^$' -fuzz '^FuzzAnyConnectStatelessCompression$' -fuzztime="$FUZZ_TIME"
          go test -run '^$' -fuzz '^FuzzLegacyDTLSRecordParser$' -fuzztime="$FUZZ_TIME"

      - name: Fuzz mihomo packet boundary
        run: go test -tags=with_gvisor ./adapter/outbound -run '^$' -fuzz '^FuzzAnyConnectPacketBoundary$' -fuzztime="$FUZZ_TIME"

      - name: Upload failing fuzz corpus
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: anyconnect-fuzz-failures
          path: |
            .anyconnect-upstream/testdata/fuzz/**
            adapter/outbound/testdata/fuzz/**
          if-no-files-found: ignore

  release-gates:
    name: ${{ matrix.name }}
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: CSTP soak
            test: TestAnyConnectCSTPSoak
            timeout: 7m
            soak: "1"
            stress: ""
          - name: legacy DTLS soak
            test: TestAnyConnectLegacyDTLSSoak
            timeout: 7m
            soak: "1"
            stress: ""
          - name: modern DTLS soak
            test: TestAnyConnectModernDTLSSoak
            timeout: 7m
            soak: "1"
            stress: ""
          - name: release stress
            test: TestAnyConnectReleaseStress
            timeout: 5m
            soak: ""
            stress: "1"
    env:
      CGO_ENABLED: 0
      GOTOOLCHAIN: local
      MIHOMO_ANYCONNECT_SOAK: ${{ matrix.soak }}
      MIHOMO_ANYCONNECT_STRESS: ${{ matrix.stress }}
    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c
        with:
          go-download-base-url: "https://github.com/MetaCubeX/go/releases/download/build"
          go-version: "1.25"

      - name: Run release gate
        run: go test -tags=with_gvisor ./adapter/outbound -run '^${{ matrix.test }}$' -count=1 -timeout=${{ matrix.timeout }}
